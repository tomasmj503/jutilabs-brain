import type { RespuestaBot } from '../types/index.js';

/** Actualiza conversaciones (contadores, tokens, costo, primera_respuesta_ms) e inserta en mensajes. TODO(qwen3-coder-flash). */
export async function registrarSalida(_r: RespuestaBot, _chatwootMessageId: number): Promise<void> {
  throw new Error('No implementado');
}
